#include <iostream>
#include <string>
using namespace std;
int main ()
{
    string try = "hello world";
    //cout << try.substr(try.length()) << endl;
    //cout << try.substr(try.length()-1) << endl;
    return 0;
}
